import React from 'react';
import { Menu } from 'react-feather';

const ButtonMenu = () => (
  <div className="menu">
    <button type="button">
      <Menu />
    </button>
  </div>
);

export default ButtonMenu;
